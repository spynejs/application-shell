# application-shell

Read [AGENTS.md](AGENTS.md), then `node_modules/@spynejs/kb/AGENTS.md` before writing any SpyneJS code — the installed `@spynejs/kb` package (stackVersion v50-r10) is the authoritative knowledge base for this codebase, and its fetch discipline (read the matching kb cards/records before authoring) applies to every task here.
