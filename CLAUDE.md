# application-shell

Read [AGENTS.md](AGENTS.md), then `node_modules/@spynejs/grammar/AGENTS.md`, before writing any SpyneJS code — the installed `@spynejs/grammar` package (stackVersion v50-r11) is the SpyneJS Grammar, the authoritative source for this codebase, and its fetch discipline (route the task through the trees, read the matching cards and records before authoring) applies to every task here.
