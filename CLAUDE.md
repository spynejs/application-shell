# application-shell

Read [AGENTS.md](AGENTS.md), then `node_modules/@spynejs/grammar/AGENTS.md` before writing any SpyneJS code — the installed `@spynejs/grammar` package (stackVersion v50-r11) is the authoritative knowledge base for this codebase, and its fetch discipline (read the matching kb cards/records before authoring) applies to every task here.
